spring:
  profiles: dev

  datasource:
    url: jdbc:h2:file:./data/mytwitter-db
    username: sa
    password:
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: update

  h2:
    console:
      enabled: true
      path: /h2-console

  thymeleaf:
    cache: false
